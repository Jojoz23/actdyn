# --- Python / tooling ---
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments (never commit; recreate with uv/pip)
.venv/
.venv*/
venv/
env/
.envrc

# --- Project artifacts (large / machine-local; regenerate from scripts) ---
# Under runs/: ignore almost everything, but keep small eval summary JSONs
# (rollout_summary.json + per-mode summary.json) for report verification.
runs/**
!runs/**/
!runs/**/rollout_summary.json
!runs/**/summary.json
datasets/
*.h5
*.hdf5
.mujoco/

# Editor / OS
.DS_Store
*.swp
.idea/
