\documentclass{article}

\usepackage{iclr2025_conference,times}
\input{math_commands.tex}
\usepackage{graphicx} % Required for inserting images
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{hyperref}
\usepackage{booktabs}
% \usepackage[parfill]{parskip}

% Replace ICLR camera-ready publication banner with course-project notice.
\makeatletter
\def\@noticestring{CSC415 course project report}
\makeatother

\title{Inference-Time Adaptive Execution for Transformer Action Chunking in Robotic Manipulation\\\vspace{0.35em}\large CSC415 course project (final report)}
\author{Zuhayr Ahmed, Charles Cheung, Lawrence Fu }
\date{April 2026}

\iclrfinalcopy

\begin{document}

\maketitle

\begin{abstract}
We study adaptive execution for transformer-based action chunking in long-horizon robotic manipulation. The policy is trained with offline imitation from demonstrations; our focus is how chunks are \emph{executed} at inference time. We compare fixed execute-all and receding-horizon execution against rule-based dynamic commits (overlap disagreement, action-change magnitude, stochastic dispersion) and an optional learned commit head trained with auxiliary labels from expert action smoothness. We evaluate on RoboMimic Lift, Can, and Square with low-dimensional state, reporting success, return, commit-length statistics, replans, policy-query cost, and optional observation noise, alongside single-step and discrete-token imitation baselines implemented in the same codebase.
\end{abstract}

\section{Introduction}

Long-horizon robotic manipulation benefits from temporal abstraction, but policies must remain reactive near contact-rich decision points (e.g., grasping/insertion). Transformer-based action chunking predicts $k$ future actions to reduce decision frequency and compounding error, but most methods use a fixed $k$ (e.g., $k \approx 100$), which can reduce reactivity and introduce boundary instability. In particular, chunk switches can cause discontinuities or chunk-boundary inconsistency/jitter in real-time execution \cite{real_time_chunking,bidirect}.

\textbf{RL framing.} We fit the chunking policy from a \emph{fixed offline dataset} of expert trajectories (imitation / behavioral cloning), then evaluate it in the same way as episodic RL in simulation: rollout return, success, and trajectory statistics. The environment remains a standard finite-horizon control problem; our contribution is the execution-time rule on top of the learned policy.

\textbf{Objectives.} We (1) implement adaptive commit-length execution using inference-time disagreement/uncertainty triggers and optional learned $\hat{m}$, (2) compare dynamic execution against execute-all, receding-horizon, and ACT-style temporal ensembling on the \emph{same} trained chunk policy, and (3) report rollout metrics plus optional Gaussian observation noise and multi-seed training scripts.

\textbf{Scope.} We use \textbf{RoboMimic} with \textbf{low-dimensional state} on \textbf{Lift}, \textbf{Can}, and \textbf{Square}. All three tasks are run under a shared ACT-Strong protocol and matched baseline evaluation pipeline.


\section{Related Work}
\subsection{Transformer-based Sequence Modeling}

This method reframes RL as a sequence modeling problem using sequences of tokens to represent state, action and reward, and uses Transformers and self-attention to capture long range dependency between those tokens.

Decision Transformer \cite{chen2021decisiontransformerreinforcementlearning} is the first proposal in this direction, and models RL as conditional sequence prediction. It models trajectories autoregressively and uses a causal Transformer to condition on past states, actions, and one desired return.

Trajectory Transformer \cite{janner2021offlinereinforcementlearningbig} extends that work and learns a generative model over environment transitions and full trajectories. States, actions and rewards are discretized and treated as tokens in the sequence model. Instead of directly producing actions, planning is performed by trajectory sampling or a search (beam search) at inference time.

These papers establish Transformers as a viable architecture for RL sequence modeling. This is a different modeling choice than the tabular or function-approximation value-based methods from standard MDP formulations (e.g., approximate $V$ or $Q$ and derive a policy), but policies from either family can still be compared fairly in the \emph{same} simulator by rollout return or success. However, these sequence-modeling approaches mainly focus on planning quality and do not explicitly address real-time execution constraints introduced by action chunking (i.e., when to switch or commit while the robot is already moving) \cite{real_time_chunking}.


\subsection{Action Chunking}

The Action Chunking mechanism provides temporal abstraction by predicting $k$ future actions at once instead of predicting single actions, which reduces the length of the horizon by $k$ times, reduces compounding error and increases stability.

Action Chunking with Transformers \cite{zhao2023} shows a Transformer-based policy specifically for robotics imitation learning. The policy predicts action chunks which are conditioned on visual observations and robot states, and demonstrates that Action Chunking can effectively model long-horizon robot manipulation performance. 

Reinforcement Learning with Action Chunking \cite{li2025reinforcementlearningactionchunking} formalizes Action Chunking in RL as "Q-Chunking". The paper does not treat RL as a sequence modeling problem and still relies on value functions and policy gradient. The policy introduces multi-step action prediction directly into the reinforcement learning objective. The predictions are made in an extended-action-space.

Bidirectional Decoding: Improving Action Chunking via Guided Test-Time Sampling \cite{bidirect} introduces a new algorithm called Bidirectional Decoding to improve the agent's ability to react to unexpected events when using action chunking. This run-time inference algorithm searches for possible predictions and chooses the optimal one based on both alignment with previous decisions and likelihood for future decisions, allowing for both long-term consistency and short-term reactivity, increasing the performance of the base generative algorithm.

Mixture of Horizons in Action Chunking \cite{mixtureofhorizons} proposes a new strategy splitting the action chunk into multiple segments with different horizons, providing both benefits of better long-term planning from longer horizons and increased precision of shorter horizons. 

These papers show that Action Chunking significantly improves long horizon performance, particularly for environments with sparse rewards or with compounding error from sequential decision making. Nevertheless, many approaches still rely on a fixed horizon $k$ and/or on smoothing strategies (e.g., temporal ensembling) to deal with chunk boundaries, which can reduce reactivity or lead to boundary discontinuities when switching occurs in real time \cite{real_time_chunking}.


\subsection{Our Work}

Most action-chunking policies use a fixed chunk length (e.g., $k \approx 100$), which improves long-horizon control but can reduce reactivity near decision points and cause jitter/inconsistency at chunk boundaries. Prior work on \emph{real-time} chunk execution addresses consistency across overlapping predictions under latency \cite{real_time_chunking}; bidirectional decoding and horizon mixtures address reactivity and multi-scale commitment in complementary ways \cite{bidirect,mixtureofhorizons}. We propose \textbf{adaptive chunk execution} in our setting: predict up to a maximum $k_{\max}$ actions with a Transformer chunk policy, but choose when to replan using inference-time disagreement/uncertainty triggers that preempt inconsistent chunk continuations.


\section{Methodology}

\subsection{Method}

We extend a Transformer chunk policy that predicts an action chunk $(a_t,\dots,a_{t+k_{\max}-1})$ (with $k_{\max}$ as a maximum bound). At inference time, we maintain an action queue of already-committed actions and decide the next commit length (how many actions to keep executing before replanning) using inference-time triggers. Crucially, \emph{this commit-length decision does not require ground-truth labels}: we use discrepancy measures between overlapping action predictions to detect inconsistent chunk boundary behavior.

\paragraph{Execution-time state and action queue.}
Our executor keeps a FIFO queue of planned actions. When the queue is empty, it plans a new deterministic action chunk from the current observation and chooses an initial commit length based on the evaluation mode (execute-all, receding-horizon, or a dynamic rule).

\paragraph{Primary dynamic rule: overlap disagreement (mid-execution preemption).}
While a queue is active, we repeatedly:
1. Predict a fresh candidate chunk from the current observation.
2. Compute an overlap disagreement score between the candidate chunk and the remaining queued actions over an overlap window of size $w$.
3. If the disagreement score exceeds a threshold $\tau$, we \emph{preempt} the current commit and trigger an immediate replan.

When a preemption happens, we also choose a new planned commit length as a decreasing function of the overlap disagreement score (higher disagreement $\Rightarrow$ shorter commits), clamped to $[k_{\min},k_{\max}]$. This directly targets chunk-boundary inconsistency/jitter by stopping early when the model would otherwise continue with a materially different action trajectory than the one already being executed.

\paragraph{Decide-at-start rule: action-change magnitude (cheap prefix selection).}
In this ablation, the commit length is decided when planning a new chunk: we compute per-step action deltas inside the predicted chunk and select the longest stable prefix whose delta magnitude stays below a threshold $\delta$; if no threshold violation occurs, we execute the full chunk.

\paragraph{Decide-at-start rule: stochastic uncertainty (sample-based prefix selection).}
Similarly, we plan a chunk but generate $S$ stochastic chunk samples and compute per-step dispersion. We then choose the longest prefix whose dispersion stays below an uncertainty threshold $\epsilon$.

\paragraph{Learned commit head (auxiliary objective).}
We optionally train a linear classifier on top of the observation encoder to predict a discrete commit class, supervised by the same longest-prefix rule applied to \emph{expert} actions in the dataset (expert action-change magnitude). At inference, mode \texttt{learned\_commit} queries this head once per replan to choose $\hat{m}\in[k_{\min},k_{\max}]$.

\paragraph{ACT-style temporal ensembling baseline.}
In addition to queue-based execution, we implement an ACT-style temporal ensembling mode (\texttt{temporal\_ensemble}): at every timestep, the policy predicts a full chunk and deposits each predicted action into a per-timestep bin; the executed action is an exponentially weighted blend over all overlapping predictions for that timestep (older predictions weighted by $\exp(-m i)$, with $m$ configurable in eval). This serves as an ACT-aligned comparator to our adaptive queue/preemption method.

\paragraph{Train--deploy alignment for chunk CVAE.}
The chunk CVAE is trained with two reconstruction paths: (i) the standard posterior path using $q(z \mid o, a)$, and (ii) a deployment path using only prior-latent decoding (no future-action encoder input), weighted by \texttt{deploy\_loss\_weight}. We also align train and rollout action spaces by applying the same output squashing (\texttt{tanh}) in reconstruction and inference. Offline evaluation reports both teacher-path loss and prior-$z{=}0$ action loss (\texttt{loss\_action\_prior\_z0}) to better predict rollout behavior.

Thresholds $(\tau,\delta,\epsilon,w)$ and $k_{\max}$ are set in versioned YAML configs (e.g., \texttt{configs/lift\_lowdim.yaml}) for reproducibility.



\subsection{Baselines and comparators}

\begin{table}[t]
\centering
\small
\begin{tabular}{@{}llp{6.2cm}@{}}
\toprule
Name & Role & Method summary \\
\midrule
BC-MLP & Single-step BC & Feedforward behavioral cloning baseline that predicts one action at a time from the current low-dimensional state. \\
ACT & Chunk transformer policy & Transformer chunking policy that predicts multi-step action sequences; execution mode (\texttt{full\_chunk}, \texttt{receding\_horizon}, \texttt{dynamic}, \texttt{temporal\_ensemble}) is the main variable. \\
kNN-BC (VINN-style) & Nonparametric imitation & Observation-space nearest-neighbor action retrieval baseline (VINN-style lightweight variant). \\
BeT-style & Discrete behavior model & Discretized-action imitation baseline using behavior tokenization with a classifier over action bins. \\
RT-1 & Visuomotor sequence model & Not reimplemented in this project; included as related work only. \\
\bottomrule
\end{tabular}
\caption{Baselines and comparators used in this report, described at the method level (without implementation-specific config details).}
\label{tab:baselines}
\end{table}

\noindent We evaluate BC-MLP, kNN-BC, BeT-style, and ACT variants under a shared simulator/metric protocol. RT-1 is discussed as related work but is out of scope for direct reruns in this state-only pipeline.

\subsection{Experiments}

We evaluate on \textbf{RoboMimic Lift, Can, and Square} with \textbf{low-dimensional observations}. We report episode success, return, horizon length, realized commit-length summaries ($\mathbb{E}[m]$, etc.), \textbf{replans per episode}, and \textbf{policy forward-query counts} (chunk forwards, stochastic multi-samples, and learned head forwards where applicable). Offline metrics include validation imitation loss (and commit CE when trained).

\textbf{Execution modes} for the chunk policy: \texttt{full\_chunk}, \texttt{receding\_horizon}, \texttt{dynamic} (with selectable rules in config), \texttt{temporal\_ensemble} (ACT-style overlap averaging), and \texttt{learned\_commit} when the auxiliary head is trained. Single-step baselines (BC-MLP, kNN, BeT-style) are evaluated with \texttt{receding\_horizon} so the policy class matches one-step execution.

\textbf{Robustness.} Config field \texttt{eval.obs\_noise\_std} adds Gaussian noise to the normalized observation vector at rollout time. We do not yet simulate object pose resets mid-episode.

\textbf{Seeds.} Training supports \texttt{--seed} and \texttt{--out-dir}; \texttt{scripts/train\_seeds.sh} loops multiple seeds. Reporting mean$\pm$std across seeds is recommended when time permits.

\section{Final Results}

Table~\ref{tab:act_main_cross_task} summarizes ACT-Strong execution-mode outcomes on each task (using the z0 default-mode snapshots for Can/Square and finalized Lift eval). Lift supports our adaptive execution hypothesis strongly. Can is mixed: dynamic under z0 is weaker than temporal ensemble/full chunk, while sampled-latent ablations show dynamic can recover substantially. Square is difficult for all ACT modes under this setup.

\begin{table}[t]
\centering
\small
\begin{tabular}{@{}lccccc@{}}
\toprule
Task / ACT mode & Success $\uparrow$ & Return $\uparrow$ & Policy queries & Replans/ep $\downarrow$ & Commit mean \\
\midrule
Lift dynamic & 1.00 & 60.00 & 400.0 & 86.65 & 4.56 \\
Lift temporal ensemble & 1.00 & 76.10 & 400.0 & 0.00 & -- \\
Can dynamic (z0) & 0.50 & 144.90 & 400.0 & 217.60 & 1.83 \\
Can temporal ensemble (z0) & 1.00 & 289.15 & 400.0 & 0.00 & -- \\
Square dynamic (z0) & 0.15 & 42.05 & 400.0 & 184.20 & 2.16 \\
Square receding horizon (z0) & 0.20 & 41.70 & 400.0 & 399.00 & 1.00 \\
\bottomrule
\end{tabular}
\caption{Cross-task ACT-Strong outcomes from finalized run artifacts.}
\label{tab:act_main_cross_task}
\end{table}

Table~\ref{tab:baseline_cross_task} reports one-step baseline results (receding-horizon eval for all baselines). Baselines are competitive on Lift, mixed on Can, and generally weak on Square, consistent with increased task difficulty in this low-dimensional setting.

\begin{table}[t]
\centering
\small
\begin{tabular}{@{}lccccc@{}}
\toprule
Task / baseline (receding) & Success $\uparrow$ & Return $\uparrow$ & Policy queries & Replans/ep $\downarrow$ & Commit mean \\
\midrule
Lift BC-MLP & 0.65 & 70.70 & 400.0 & 399.0 & 1.00 \\
Lift kNN-BC & 0.75 & 44.40 & 400.0 & 399.0 & 1.00 \\
Lift BeT-style & 0.75 & 127.30 & 400.0 & 399.0 & 1.00 \\
Can BC-MLP & 0.00 & 0.00 & 400.0 & 399.0 & 1.00 \\
Can kNN-BC & 0.30 & 58.65 & 400.0 & 399.0 & 1.00 \\
Can BeT-style & 0.30 & 33.75 & 400.0 & 399.0 & 1.00 \\
Square BC-MLP & 0.05 & 0.30 & 400.0 & 399.0 & 1.00 \\
Square kNN-BC & 0.35 & 43.00 & 400.0 & 399.0 & 1.00 \\
Square BeT-style & 0.05 & 5.60 & 400.0 & 399.0 & 1.00 \\
\bottomrule
\end{tabular}
\caption{Cross-task baseline summary after baseline-eval recovery pass.}
\label{tab:baseline_cross_task}
\end{table}

Across Lift multi-seed ACT-Strong runs (seeds 0/1/2), success remains $1.00 \pm 0.00$ (mean $\pm$ std). For Can and Square, multi-seed summaries are available in task seed folders and are consistent with the single-run trend: Can is mixed and sensitive to execution/latent settings, while Square remains challenging for all tested policies in this configuration.


\section{Discussion}
\paragraph{Hypothesis outcome.} Our central hypothesis is \emph{partially supported}. Adaptive execution is strongly supported on Lift, where dynamic commits match top success while reducing replans relative to receding horizon. On Can, behavior is mixed and sensitive to rollout setting (notably z0 versus sampled-latent ablations). On Square, all methods in our low-dimensional setup remain weak, indicating task difficulty dominates any single execution-rule choice under current training/data conditions.

\paragraph{Limitations.} Rollout evaluation requires the optional RoboMimic/MuJoCo stack (we use a separate Python~3.10 environment in practice). Lift success can be dominated by imitation quality; execution-mode differences can appear in commit statistics even when success is low. RT-1, full VINN, and full BeT are not reproduced at original scale.

\paragraph{Failure modes.} If disagreement/uncertainty thresholds are miscalibrated, the policy may replan too frequently (hurting stability) or too late (reducing reactivity). Threshold sweeps are supported by editing YAML and re-running evaluation.

\section{Conclusion}
We implemented inference-time adaptive commit-length execution for an ACT-style chunk policy, with heuristic rules, an optional learned commit head, imitation baselines (MLP BC, kNN, discrete-token BC), and shared rollout instrumentation. Final cross-task results support the proposal strongly on Lift, show mixed behavior on Can (including strong sampled-latent dynamic ablations), and reveal that Square is substantially harder under this low-dimensional setup. RT-1-scale models remain contextual citations rather than rerun baselines in this pipeline.

\section{Team Contributions}
In equal parts: Coding, Training, Inference. 
Charles: Ablations. 
Zuhayr: Analysis. 
Lawrence: Evaluation, benchmarking. 

\begin{itemize}
    \item Week 1: Architecture design, coding
    \item Week 2: Coding
    \item Week 3: Inference,  ablations, evaluation
    \item Week 4: Analysis, writeup
    \item Week 5: Prepare Presentation
\end{itemize}


\appendix

\section{AI Usage Declaration}

We used AI assistants (Cursor/ChatGPT) as coding and writing support tools: to refine prose, suggest command-line workflows, and help diagnose implementation issues. All experiment design decisions, code integration, run execution, result validation, and final report content were reviewed and finalized by the authors.



\bibliographystyle{iclr2025_conference}
\bibliography{refs}





\end{document}
